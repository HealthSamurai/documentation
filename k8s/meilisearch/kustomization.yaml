apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitbok

resources:
  - configmap-grafana-dashboard.yaml
  - configmap.yaml
  - configmap-reindex-env.yaml
  - configmap-reindex-script.yaml
  - cronjob-reindex-fhirbase.yaml
  - cronjob-reindex.yaml
  - service.yaml
  - serviceaccount.yaml
  - servicemonitor.yaml
  - statefulset.yaml

commonLabels:
  app.kubernetes.io/name: meilisearch
  app.kubernetes.io/part-of: documentation-search

configMapGenerator:
  - name: meilisearch-scraper-config
    files:
      - config.json=./config.json
  - name: meilisearch-scraper-config-fhirbase
    files:
      - config.json=./config-fhirbase.json

generatorOptions:
  disableNameSuffixHash: false
