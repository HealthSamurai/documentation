apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ns.yaml
- deployment.yaml
- service.yaml
- ingress.yaml
- ingress-fhirbase.yaml
- ingress-docs-aidbox.yaml
- meilisearch/
# Temporarily disabled until app is deployed with /metrics endpoint
# Uncomment after successful deployment and verification
# - monitoring/
