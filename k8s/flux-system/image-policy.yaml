---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: gitbok
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: gitbok
  policy:
    semver:
      range: '>=1.0.0'