# Release Notes

## 0.2.0
* Add release cycle documentation
* Add date sorting to frontend
* Fix stability when using AuditBox with multiple replicas

## 0.1.0
* Add facet search to "action" and "outcome" fields
* Add a script to export events from Aidbox into Auditbox
* Fix "patient" facet search not working if display name is missing from role
* Fix export ignoring full text search query
* Improve demo data
